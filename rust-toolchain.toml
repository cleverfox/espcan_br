# Classic ESP32 is Xtensa -> the `esp` Rust toolchain fork (espup install).
# rust-src is needed for build-std (core+alloc) on xtensa-esp32-none-elf.
[toolchain]
channel = "esp"
components = ["rust-src"]
